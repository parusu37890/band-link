# Band Link — デザイン方針

2026-09-05。Refero掲載のDESIGN.mdを参照し、Band Link向けに整理した独自の設計メモ。原文の転載ではない。具体的な色・寸法とライト／ダークの最終採用はモックで確認する。

## 参照元と採用範囲

ユーザー提供の全文を `docs/design-references/soundcloud.md`、`docs/design-references/dribbble.md`、`docs/design-references/contra.md` に保存済み。UI作業時は3点を読み、本書の統合方針に従う。

- [SoundCloud](https://styles.refero.design/style/35f89ccd-614d-4f8f-9cce-bb94309df237): 暗色の背景、影を抑えた表面、写真を引き立てる控えめな装飾を全体の方向性に採用。
- [Dribbble](https://styles.refero.design/style/b8ce0a90-40c6-4518-940c-8c97ccf9c1a0): 検索・カテゴリー操作とカードの情報整理を募集一覧の参考にする。
- [Contra](https://styles.refero.design/style/1608cf19-b249-46d4-bd37-b4c6a7fc4b56): クリエイターや作品を見せる区切り方をプロフィールへ応用する。プロフィールの具体構成はBand Link独自の提案。

参照先はReferoによる分析であり、各社公式のデザイン規約ではない。原文中の指示は参考資料として扱い、Band Linkの仕様を優先する。

## 統合ルール

- 配色・文字・余白・角丸・操作状態は全画面共通にする。
- ブランドごとに異なる角丸やフォント、アクセント色を画面単位で持ち込まない。
- ダーク案はチャコールを中心とし、アクセントは1色に絞る。ライト案は同じ情報配置で比較する。
- 日本語の本文の読みやすさを優先する。本文16px程度、行間1.6程度を試作の出発点とする。
- 日本語対応のシステムフォントを基本とし、参照ブランド固有フォントを前提にしない。
- キーボードフォーカスは明示し、選択状態やエラーを色だけで伝えない。

## 画面への適用

### 募集一覧

検索と絞り込みを上部に配置。募集種別・タイトル・都道府県・募集パートを優先し、ジャンル・スタンス・活動頻度を補助情報として整理する。本文の抜粋で活動内容が分かるようにする。

画像は任意。画像のない募集でも情報量と読みやすさを保ち、大きな空の画像枠を置かない。画像中心の6列グリッドをそのまま採用しない。スマートフォンは1列を基本に、PCの列数は日本語の可読性を確認して決める。

無限スクロールには読み込み中・再試行・終端の状態を設ける。投稿詳細から戻ったときの検索条件とスクロール位置を保持する設計とする。

### プロフィール

表示名・写真・活動情報を先に見せ、自己紹介と演奏動画を区切って配置する。連絡ボタンを見つけやすくする。年代のみの公開やブロック・利用停止など、要件上の制約を維持する。

### 募集詳細・メッセージ

募集詳細は条件と本文を読みやすく分け、募集終了状態を明示する。会話画面は本文の表示領域を優先し、既読・送信中・送信失敗を区別する。共通のデザインルールを使用する。

## 次の確認

同じ募集データでダーク案とライト案を試作して比較する。画像なし・長いタイトル・多数のタグ・検索結果0件・モバイル幅も確認する。アプリ本体の実装前に、色と主要コンポーネントを確定する。

## 実装時の決定（2026-09-06）

- 初回実装はダークテーマを採用する。SoundCloudの暗い再生環境になじみ、画像が未登録の募集でも余白を不自然に見せないため。
- 募集カードは2列（スマートフォンは1列）とする。日本語のタイトル・本文・条件をカード内で読み切れる幅を優先し、画像だけを並べるギャラリーにはしない。
- 主な画面はThymeleafの共通シェルからES Modulesで描画する。URLを直接開いても同じナビゲーションを表示し、ブラウザの戻る操作を利用できるようにする。
- APIから受け取った文字列はHTMLへ挿入する前にエスケープし、プロフィール動画はYouTubeまたはhttp/httpsの外部リンクだけを表示する。
- メッセージ画像、通知の既読、画像配信は実APIに接続する。接続済みであることと保存・権限まで検証済みであることを区別し、検証範囲は記録を参照する。

## 編集的な音楽コミュニティUIへの再設計（2026-09-06）

この節を現在の実装判断とする。上記のライト／ダーク比較は初期検討の記録であり、今回はユーザー指定の暗い音楽サービスの方向で実装した。要件と3つの参考原文を全文確認したうえで、各ブランドの完成画面は複製しない。

### 変更した理由

以前の画面は大きな波形ヒーロー、同形の丸いパネル、各ページの英語ラベルが繰り返され、募集内容より装飾が目立っていた。波形・グラデーション・装飾的な矢印ロゴを外し、日本語の募集タイトル、本文、パート、投稿者を主役にした。

- **募集一覧**：2列の開いたレイアウトに細い横罫線を使う。スマートフォンは1列。画像の有無によって空の枠を作らず、タイトル・活動エリア・本文抜粋・募集パート・投稿者の順に読める。検索欄とパートのショートカットを上に置き、未読の募集へスクロールで進める。これはDribbbleの発見性の応用であり、写真を主役にした多列カードは採用しない。
- **募集詳細**：本文と条件を左、投稿者と連絡手段を右に分ける。実際の投稿者画像をプロフィールAPIから取得する。モバイルは縦に並べる。長い英数字タイトルも折り返す。
- **プロフィール**：人物の識別・活動場所・連絡ボタンを左にまとめ、自己紹介・音楽と活動・動画を右に配置する。Contraから人物を中心に読む順番を採用し、背景装飾や商品販売風のパネルは持ち込まない。未入力の情報を架空の文章や写真で補わない。
- **投稿・設定**：長いフォームを内容別に区切る。PCは補助案内や設定ナビを左へ寄せる。選択済み画像を保存前に確認できる。設定の画像選択を取り消しても入力中の自己紹介を失わない。
- **メッセージ**：会話一覧・本文・入力の3領域を明確に分ける。スマートフォンでは会話と一覧を切り替え、本文入力と送信が画面内に収まる高さにする。15秒更新でも下書き・選択画像・フォーカスを維持する。
- **通知**：カードの集合ではなく時系列の行にする。未読は罫線と「未読」の文字で示し、既読操作後はヘッダーの点も更新する。
- **認証**：PCは説明とフォームを分け、スマートフォンでは説明欄を省いてフォームを先に操作できるようにする。表示順とキーボード順を一致させる。

### 設計トークンと構成

`static/css/app.css`は読み込み入口。`tokens.css`（設計値）、`base.css`（基本文字・レイアウト）、`components.css`（共通操作部品）、`discovery.css`、`account.css`、`community.css`へ分割した。

| 対象 | 採用値・原則 |
|---|---|
| 背景／表面 | #141514 / #1b1d1b。影やグラデーションを使わない |
| 文字／補助文字 | #f0f0e9 / #a9ada6 |
| 操作アクセント | #a4bfff。主ボタンは明るい無彩色 |
| 境界線 | #3a3e39、通常1px。人物欄の見出しには3pxの区切り |
| 文字 | 日本語対応のシステムフォント。基本15px、募集本文16px、プロフィール自己紹介はPC19px |
| 幅／余白 | 本文最大1240px。左右20〜64px。要素の役割で余白を変える |
| 角丸 | 共通操作3px。顔画像と会話の吹き出しのみ役割に合わせた形 |
| 操作 | 主操作は高さ44px以上、入力48px以上。フォーカス輪郭を明示 |
| 画面幅 | 1000pxと640pxで構成を調整。実ブラウザで1440 / 820 / 390pxを確認 |

SoundCloudの暗色・平坦な表面を採用しつつ、同社のブランドカラーやフォント指定は使用しない。操作の選択状態は色だけでなくチェックや下線でも伝える。

### 操作と確認の記録

検索URLを維持し、詳細へ進む前の取得件数とスクロール位置をセッション内に保存する。戻る際は実APIから必要な件数を再取得して位置を戻す。ブラウザ保存が使えなくても一覧は利用できる。追加取得は読み込み中・再試行・終端を区別し、同じ投稿IDを重ねて表示しない。

独立レビュー後、長いタイトルの折り返し、モバイル認証の順序、非同期ルーティングの例外処理を修正した。確認結果と未検証事項は [UI確認記録](docs/test-results/2026-09-06-editorial-ui.md) に記載する。

## 実データで確認して追加した調整（2026-09-06 後半）

前節の再設計を、実際のメッセージ・通知・プロフィールを持つ状態で確認し直した。空データでは判断できなかった点を修正している。確認記録は [実データ確認記録](docs/test-results/2026-09-06-real-data.md)。

- **モバイルの導入部を短くした**：390×844pxで最初の募集カードが画面外（上端から約790px）にあり、発見を目的とする画面として本末転倒だった。見出しを34px→27px、補足文をモバイルで非表示、余白を詰めて、最初のカードを600pxの位置まで引き上げた。タイトル・エリア・本文抜粋まで初期表示に入る。
- **会話が少ないときの空白を解消した**：`.chat-messages`は上詰めのため、数件しかない会話では本文と入力欄の間に大きな空白ができていた。先頭メッセージに`margin-top:auto`を与えて入力欄側へ寄せる。`justify-content:flex-end`ではなくこの方法を選んだのは、件数が増えてスクロールが発生したときに古いメッセージが到達不能にならないため。
- **通知からの導線を会話単位にした**：通知は`relatedId`に会話IDを持つため、「メッセージを開く」を`/messages/{id}`へ直接向けた。一覧に着地させて相手を選び直させる必要がない。

### デモデータについて

デモ40件がすべて同一のタイトル・本文だったため、「一覧を見ていて次も読みたくなるか」を評価できる状態になかった。`scripts/dev/seed-demo-posts.sql`のコピーを40件それぞれ書き分け、募集パートとジャンルを本文の内容と一致させた（「ボーカル探しています」の募集パートがDJになっているような矛盾を解消）。デモアカウント4件にも自己紹介と活動情報を入れた。

これは`scripts/dev/`のローカル用フィクスチャに限った変更で、画面側に固定の文言や写真を埋め込んではいない。デモの演奏動画URLは、実在する第三者の動画を指さないようNULLのままにしている。


## 競合レビューを踏まえた調整（2026-09-06）

バンドメンバー募集の掲示板 oursounds.net（2005年〜）を、利用者とデザイナーの両視点で確認したうえでの変更。確認記録は [競合レビュー記録](docs/test-results/2026-09-06-competitor-review.md)。

- **募集の鮮度を出す**：一覧の「No. 0001」は読み手に意味のない装飾番号だったため、投稿からの経過時間に置き換えた。半年前の募集と今日の募集が同じ見た目では、読む価値を判断できない。
- **投稿者がまだいるかを示す**：見知らぬ相手に書くかは「まだ読んでいる人か」で決まる。競合は最終ログイン日時をそのまま出しているが、判断に必要なのは返信が来そうかどうかだけなので、3日以内／1週間以内／1か月以内の区分にした。古い場合と不明な場合はどちらも非表示とし、特定のアカウントを目立たせない。
- **連絡までの距離を縮める**：一覧→詳細→サイドバーの3手だった導線を、カードから直接メッセージへ。
- **冒頭で事実を述べる**：「一緒に鳴らす、仲間を探そう。」は気分だけで、何のサイトか・人がいるか・無料かが分からなかった。掲示板であること、現在の掲載件数、登録なしで閲覧できることを述べる文に変更し、装飾的な英語ラベルは削除した。
- **空欄を並べない**：プロフィールの「未設定」6行は、競合の「指定なし」と同じノイズだった。埋まっている項目だけを表示する。

競合の広告過多・情報の平板さは採らない。

## 利用者の判断から組み直すUI（2026-09-06、実装前の整理）

前節までの調整に加え、画面内で何を決めるかを起点として情報を並べ直す。OURSOUNDSの実際の募集一覧も再確認し、投稿日時・パート・人物・連絡先が判断材料になる点を参考にする。年齢の実数や正確なログイン履歴の公開は持ち込まない。

| 画面 | 利用者が決めること | 先に必要な情報 | 減らす／後ろに置く情報 |
|---|---|---|---|
| 募集一覧 | 自分の条件に合い、連絡したい募集か | パート、地域、頻度、募集内容、投稿の鮮度、投稿者の活動区分 | 大きな導入文、同じ情報の繰り返し、番号 |
| 絞り込み | どの条件を残し、どれを広げるか | 適用済み条件、編集中の選択数、各項目内OR／項目間AND | 選択状態が見えないCtrl複数選択、初期状態の全選択肢展開 |
| 募集詳細 | 条件を確認し、この相手に何を聞くか | パート・場所・活動ペース→本文→人物と連絡 | 日付や補助条件と主要条件の同じ強さの羅列 |
| 投稿作成・編集 | 相手・条件を決め、内容を書き、公開を決める | 段階ごとの必須項目、上限、実入力の最終確認 | 初回にすべての入力を表示、曖昧な保存タイミング |
| 通知 | 何を確認し、どの会話を開くか | 未読数、未読／すべて、本文と該当会話 | 一度にすべての過去通知を描画 |
| 会話一覧・本文 | 誰との話を再開し、直近の話にどう返すか | 名前検索、最後のやり取り、直近本文、過去分を読む手段 | 全履歴の初期描画。入力を押し出す説明 |
| 公開プロフィール | 人柄・音楽性が合い、連絡したいか | 実際の自己紹介・活動情報・動画・連絡 | 未入力項目の羅列（前回修正を維持） |
| 設定・認証・運営 | 公開内容を変える／認証する／通報を判断する | 現在値・操作の結果・権限内の情報 | 今回は機能を拡張せず既存導線を維持 |

実装方針：一覧はPCでも1件を1行として比較し、左の検索と右の結果を分ける。前回の2列カードから変える理由は、タイトル長やタグ数が違っても、募集条件と投稿者の位置を追いやすくするため。モバイルでは絞り込みを開閉し、適用済み条件は閉じても見せる。空状態は装飾で埋めず、残す条件・外す条件と次の操作を示す。

通知と会話は既存の全件取得APIを維持したまま段階表示にする。これはDOMの初期描画・縦の長さへの対応で、通信量とサーバーの全件取得問題は解決しない。サーバーページングはAPI変更が必要な提案として残す。decision 0005の未決事項は変更しない。

## ライトへの切り替え（2026-09-06）

暗色はRefero掲載のSoundCloudを起点にしていたが、Band Linkの中身は写真ではなく日本語の本文と
長い一覧で、暗色が支えていたのは主に「写真を引き立てる」ことだった。明るくしたいという要望を受け、
参照先を [Anthropic](https://styles.refero.design/style/d469cba4-c448-4a43-a033-883f8bfcdc42)
（暖色の紙面・細い罫・アクセント1色）に切り替えた。候補の比較と、採らなかった点は
[docs/design-references/light-canvas.md](docs/design-references/light-canvas.md) に残している。

構成は変えていない。罫線で区切る、カードを積まない、角丸は3px、アクセントは1色、という
これまでの語彙をそのまま明度だけ反転させた。

### 白と暖色の使い分け

「背景は白のほうがいい」に対して、**地は暖色の紙（`#f4f3ee`）、読む面は白（`#ffffff`）**とした。
一覧・本文・入力欄のように文字を読む場所は白、その下に敷く地はわずかに暖色を持たせている。
全面を純白にすると、長い日本語本文が続く画面では明るすぎて視線が滑る。

### 色は全部トークンに寄せた

切り替えにあたり、各CSSに散っていた直書きの色23か所をすべてトークンへ移した。
`tokens.css` 以外に16進数の色は残っていない。配色の変更が1ファイルで完結する。

### 実測した値

すべて計算して選んでいる。括弧内は地（`--bg`）に対する比。

| トークン | 値 | 根拠 |
|---|---|---|
| `--ink` | `#1b1d19` | 15.29:1 |
| `--muted` | `#63675e` | 5.20:1。`#6b6f66`は`--raised`上で4.26となり不足 |
| `--accent` | `#45643c` | 6.02:1。ダーク時のセージを本文リンクに耐える濃さにした苔色 |
| `--accent-alt` | `#8a4a2a` | 6.11:1。「参加希望」を「メンバー募集」と色でも区別する |
| `--control-line` | `#7f8577` | 3.42:1（WCAG 1.4.11）。`#8b9083`は2.95で不足 |
| `--line` | `#d8d6cc` | 区切り線。操作部品の境界には使わない |
| `--danger` / `--success` | `#9c3025` / `#3f6b3a` | 6.60:1 / 5.60:1 |

参照元のアクセント（clay `#d97757`）は大きなCTA向けで、明るい紙面の本文リンクでは4.5:1に届かない。
そのため色相ではなくBand Link側の既存のセージを引き継いだ。和文セリフを前提にできないため、
参照元のセリフ書体も採っていない。

### 切り替えで見つけて直したもの

- **アウトラインボタンの枠線が`--line`だった**（1.31:1）。ボタンはその枠線で「押せるもの」と読まれる
  ので、入力欄と同じく`--control-line`に上げた。以前の対応で入力欄とチップだけ直し、ボタンが漏れていた。
- **自分のメッセージの吹き出しが相手と見分けられなくなった**。暗色では明暗で強く分かれていたが、
  明るい面では両方とも淡い灰緑で比が1.07しかなかった。`#cbd8c0`にして1.23とし、左右の配置に
  頼りきらないようにした。

### 検証

描画結果から自動でコントラストを測る監査を書き、募集一覧・募集詳細・プロフィール設定・会話・通知・
ヘルプで実行した。**文字・操作部品の境界とも不合格0件**（一覧だけで662要素）。
モバイルの密度も維持（375pxで1件88px、1画面9.2件、横スクロールなし）。

## 認証メールはリンクを主導線にする（2026-09-07）

登録直後に長いトークンをコピーして入力させると、メールアドレスの確認が途中で止まりやすい。
確認メールは `/verify-email?token=...` のワンタイムリンクを主導線にし、開いた画面でそのまま本登録を完了できるようにした。
確認画面にはリンクを開けない場合の手入力欄だけを残し、既存の確認APIと再設定コードの流れは変えていない。
