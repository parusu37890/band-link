# 2026-09-15 最新仕様との差分

状態: 2026-09-15時点で単体98/98、PostgreSQL結合19/19、業務状態マトリクス14,227/14,227を実行済み。公式Playwright MCPによる最新UI回帰は未実行です。

この文書は、キーワード検索削除、投稿時刻の4週間表示上限、募集一覧のheader直下見出し削除、募集・加入希望の各1件制限を基準に、現行実装・既存テスト・設計記録へ残る不整合を整理します。過去の実行証跡は書き換えず、再実行結果で更新します。

|変更|現状の不整合|影響と必要な対応|
|---|---|---|
|キーワード検索削除|UI・検索サービス・履歴正規化を選択式条件だけへ統一し、keywordは後方互換の受け口で無視する実装へ更新済み。IT-017、IT-032、業務マトリクスは現行契約でPASS。|API仕様書に「keywordは無視」と明記し、将来400へ変更する場合は別リリース要件として扱う。|
|キーワード検索削除|`docs/decisions/0002-freetext-area-search.md` はタイトル・本文・補足エリアのキーワード検索を採用した記録のまま。|履歴を改変せず、新しいADRで0002を明示的にsupersedeする。|
|投稿時刻4週間上限|画面実装は `ui.js` の `relativeTime` と `discovery.js` の一覧・詳細表示で上限を実装済みだが、境界を直接検証する自動テストがない。|1分、1時間、1日、1週、3週、4週、4週超を固定時計で確認する自動テストを追加し、ST-019の実行証跡を残す。|
|header直下見出し削除|画面実装は `recruitment-search.js` から重複見出しを削除済みだが、`DESIGN.md` は画面タイトル「バンドメンバー募集」を残す方針を記載する。|DESIGNの判断記録を最新方針へ追記し、ST-027/055でdesktop/mobileの見出し階層と一覧冒頭を確認する。|
|募集・加入希望を各1件|`PostService` と `PostRepository` は種別単位の存在確認へ更新済み。通常のPostgreSQL結合と業務状態マトリクスはPASS。ただし `PostMessageStateAdapter` に旧状態名が残り、同時要求時のDB一意性は未確認。|IT-014を同種別拒否・異種別許可の2段階へ更新し、全件JUnitアダプターを新しい `same-type-open` / `other-type-open` 状態へ合わせる。NFT-003では同時要求時のDB結果も種別ごとに確認する。|
|現行リリース判定|単体・結合・業務マトリクスは2026-09-15に現行コードで再実行しPASS。|公式Playwright最新UI回帰、ユーザーテスト、NFT、同時投稿のDB一意性を完了するまでGOにしない。|

## 未確定事項

1. 廃止済みの `keyword` query parameterを直APIで受けた場合に、400で拒否するか無視するか。UIからは送信されないことを確認済みですが、`PostSearchCriteria`/Controller/Serviceには旧パラメータ処理が残っています。API契約を決めて実装とIT-017/021を揃える必要があります。
2. 投稿時刻の4週間上限は一覧カードと投稿詳細上部に適用すると要件へ記載しました。詳細下部の正確な日時表示を残すかは未確定です。
3. 種別ごとの公開1件制限を同時要求でも保証するDB制約・ロック方式は未確定です。サービス層の事前存在確認だけでは競合時に2件作成される可能性があります。
4. 募集一覧から可視のページ見出しを除いた後、アクセシビリティ用の非表示`h1`を置くか、header内のサイト名でランドマークを構成するかは未確定です。


