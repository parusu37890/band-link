# リリース候補システムテスト（2026-09-08）

## 対象

- リビジョン: `a626c1f`（作業ツリーに性別表記・デモシード修正を適用）
- 環境: Windows 11、Java 26.0.2、PostgreSQL 18、Spring Boot ローカル起動（`http://localhost:8080`）
- ブラウザ: Codex内蔵ブラウザのPlaywright操作API
- 公式Playwright MCPサーバーはこのセッションでは利用できないため、以下はPlaywright MCP実行済みとは扱わない。

## JUnit

修正前の`target/surefire-reports`（01:12:18）を集計した。

- 37件実行
- 失敗 0、エラー 0、スキップ 0

修正後の全体実行は37件中3件がPostgreSQL認証エラー（`DB_PASSWORD`未設定）となった。DB非依存の追試13件は成功している。

## ブラウザST

| ID | 操作 | 結果 |
|---|---|---|
| ST-01 | 未ログインで募集一覧を開く | 合格。200、初期12件、ログイン導線を表示 |
| ST-02 | 募集を投稿者ログイン順へ変更 | 合格。`/posts?sort=login`、並び順と活動区分を表示 |
| ST-03 | キーワード`ST`で検索 | 合格。1件に絞り込み |
| ST-04 | 東京都＋ギターで複合検索 | 合格。1件、適用条件をチップ表示 |
| ST-05 | 0件検索 | 合格。専用空状態と「すべての募集を見る」を表示 |
| ST-06 | 詳細表示→戻る | 合格。検索条件と件数を復元 |
| ST-07 | 「参加希望」フィルター | 合格。10件、全件が参加希望 |
| ST-08 | ログイン・登録・メール確認・再設定画面 | 合格。フォームと導線を表示 |
| ST-09 | `demo01@bandlink.local`でログイン | 合格。認証後ナビゲーションを表示 |
| ST-10 | 自分の募集・公開プロフィール・通知・会話・設定・ブロック・新規投稿 | 合格。各画面を表示、コンソールエラーなし |
| ST-11 | 会話本文と以前の30件 | 合格。30件→60件へ追加表示、既読表示あり |
| ST-12 | 通知一覧と未読フィルター | 合格。44件表示、未読0件の空状態を表示 |
| ST-13 | 募集作成を空欄送信 | 合格。活動エリア必須エラーで停止、投稿なし |
| ST-14 | 募集・プロフィールから通報フォームを開く | 合格。理由入力、キャンセル導線を表示 |
| ST-15 | 管理画面 | 合格。通報理由と通報対象メッセージの本文・画像だけを表示 |
| ST-16 | 未ログインの管理画面/API | 合格。画面302、API 403 |
| ST-17 | メッセージ画像の権限 | 合格。未ログイン401、非参加者403、管理画面の通報対象画像は表示 |
| ST-18 | 自分自身へのメッセージ | 合格。禁止理由を表示 |
| ST-19 | 390×844の一覧・詳細・プロフィール・会話 | 合格。横はみ出しなし、コンソールエラーなし |
| ST-20 | ログアウト | 合格。確認ダイアログ後にログイン画面へ遷移 |
| ST-21 | ログイン後に`ST`検索し検索履歴APIを確認 | 合格。履歴1件を保存、条件文字列を保持 |

## API・ログ確認

- `/api/auth/me` 未ログイン401、`/api/csrf` 200、`/api/posts/page` 200、LINE無効状態200を確認。
- 公開プロフィールJSONにメールアドレス・パスワードを含まないことを確認。
- 既に公開投稿があるユーザーの新規投稿は409、31文字タイトルは400。
- ログイン後の検索は`/api/search-history`へ1件保存された。
- ログファイルを検索し、パスワード・メール本文・メッセージ本文の記録は見つからなかった（`/password-reset`のパス名のみ）。

## リリース判定に影響する指摘

1. **初回P1: デモユーザーの公開投稿数が要件違反。** `demo01`の「自分の募集」に公開中10件が表示された。新規投稿API自体は409で拒否したため、seedを修正し、再実行時に最新1件以外を`CLOSED(MANUAL)`へ揃える処理を追加した。
2. **初回P1（修正済み）: 既存プロフィールの性別値がUIと不一致。** UI、DTOバリデーション、プロフィール完成判定、テスト、設計資料を「男性」／「女性」に統一した。シードのデモプロフィールも同じ値を設定している。
3. **初回P1（追試合格）: 日本語キーワード入力。** 修正後のPlaywright操作APIでは`ギター`がそのままURLに反映され、8件の結果を表示した。前回の`ギターｓさ`への変化は再現しなかった。

## 未実施

公式Playwright MCPでの同一ST、メール受信を使う確認リンク・パスワード再設定、実LINE OAuth、画像アップロードの全上限ケース、第三者アカウントによる全権限組み合わせ、利用停止・解除、本人退会、Safari・実機・スクリーンリーダー、Elasticsearch/Kibana。

## 判定

主要画面と日本語検索の追試は合格した。デモシードを実DBへ再実行した後の件数確認、`DB_PASSWORD`を設定した環境での全JUnit、公式Playwright MCPによる同一STが残っているため、現時点ではリリース可とは判定しない。

## 追試（修正後）

- 登録・設定画面の性別ラジオボタンが「男性」「女性」と表示され、`demo01`の既存プロフィールでは「男性」が選択済みになることを確認した。
- プロフィール編集をページキャンバス（`--bg`）へ揃え、設定ナビゲーションの選択線とセクション境界を整理した。画像欄のプレビュー・選択・補足・削除を整列し、幅512pxで横スクロールが発生しないこと（`scrollWidth=497`、`innerWidth=512`）を確認した。
- `/users/136`の公開プロフィールも本文・音楽と活動の各面をページ背景と同じ色に揃え、幅512pxで表示を確認した。
- 公開プロフィールの「メッセージを送る」から新規相手の`/messages?to=3`へ遷移し、宛先名・「会話をはじめる」案内・メッセージ本文入力欄・画像添付・「送信」ボタンが表示されることを確認した。空の受信箱用クラスが新規DMの送信欄を隠していたため、宛先を解決した画面では`is-empty`を解除するよう修正し、メッセージ画面モジュールにもキャッシュバスターを付けた。
- DMの会話一覧・本文・入力欄をページ背景（`--bg`）へ揃え、送信ボタンの文言を「送信」へ統一した。画像のMIMEタイプが空・`image/jpg`・`application/octet-stream`になる環境でも拡張子と画像データを検証して保存できるようにした。
- DMのメッセージ画像はサムネイルをクリック（キーボードではフォーカス後にEnter）すると、閉じるボタン付きのダイアログで拡大表示する実装を追加した。
- Playwright操作APIで`ギター`を入力して検索し、URLが`/posts?keyword=ギター`となり8件の検索結果が表示されることを確認した。前回の入力文字列変化は再現しなかった。
- デモシードには、ユーザーごとに最新の1件だけを`OPEN`として残し、それ以外を`CLOSED(MANUAL)`に揃える処理を追加した。シード再実行後に件数を再確認する必要がある。
- デモシードの`last_seen_at`にオンライン中（5分以内）のユーザーを2名追加し、ログイン順表示でオンライン表示を確認できるデータ条件を用意した。実DBへの反映はシード再実行後に確認する。
- 修正後のMaven実行は、実行環境に`DB_PASSWORD`が渡っておらずPostgreSQL認証（SQLState 28P01）でコンテキストテスト3件がエラーになった。コンパイルは完了しているため、DBパスワードを設定した同じ端末で`mvnw.cmd -B clean test`を再実行する。
