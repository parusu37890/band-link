# ST（Playwright MCP）— 2026-09-14 PW-F（ST-044..053: block・report・admin・feedback・notifications）

`docs/test-plan/playwright-mcp-spec.md` のバッチ定義 PW-F に対応。公式 `@playwright/mcp`（`mcp__playwright__*`）で実施。PW-E（`09055d4`、ST-035..043・ST-033・ST-034）の続き。ユーザーからPW-E完了後は止まらずPW-Fへ継続するよう指定があったため実施。

**状態: PW-F（ST-044..053）を全件実施・記録済み。新規バグは発見されなかった（ソース変更なし）。**

## 実施環境

PW-A〜Eと同一の`band_link_release_test`（`scripts/test/seed-release-users.sql`で都度再投入）。viewport 1440×900（デスクトップのみ）。admin/report/block系の権限チェックはPW-E同様、実際の操作で確認しラバースタンプしない方針を徹底した（DM画像バグがまさにこの種の領域で見つかったため）。

## 結果サマリ

| ケース | 結果 | 備考 |
|---|---|---|
| ST-044 profileから確認後にblockし管理画面へ移る | PASS | U09→U10（seed済みblockを一旦解除した状態から）で「この人をブロック」→確認ダイアログ→実行すると、DBに1件だけblock行が作成され`/settings/blocks`へ自動遷移。会話画面では「ブロック中のため、メッセージを送信できません。過去の会話は引き続き確認できます。」に置き換わり、過去メッセージは表示されたまま送信フォームだけ無効化されることを確認 |
| ST-045 block解除後に一覧とDMが復旧する | PASS | U09の設定画面からU10のブロックを解除→確認ダイアログ→実行するとDBのblock行が削除され、一覧から「ブロック中のユーザーはいません。」表示に変化。解除直後に実際にDMを送信して成功することも確認（見た目のUI状態だけでなく、実際の送信が復旧していることを検証） |
| ST-046 投稿・利用者・DMを理由付きで通報する | PASS | 投稿（P002）・利用者（プロフィール）・DM（停止中ユーザーの既存メッセージ）の3種すべてをkeyboard操作（Enterでダイアログを開く→Tabで理由欄へ→Tab+Enterで送信）のみで通報。DBに`target_type`ごと1件ずつ、理由文言も正しく保存され、完了toastが毎回表示されることを確認。DM通報では理由欄をちょうど1000文字（上限）で入力し、切り詰めなく保存されることも確認 |
| ST-047 管理者がstatus別通報とsnapshotだけを確認する | PASS | 対応状況フィルタ（未対応/確認済み/対応不要/対応済み）を切り替えると該当ステータスの通報のみ表示されることを確認（未対応4件→確認済みに切替で対象の1件のみに変化）。メッセージ通報では通報時点の当該メッセージ本文のみが表示され、「通報時点の内容です。前後の会話は表示されません。」の注記どおり同一会話内の前後メッセージ（seedで意図的に「前文非公開」「後文非公開」という名称で用意されている）が一切表示されないことを確認 |
| ST-048 管理者が通報状態変更・投稿非公開・利用停止解除する | PASS | 3種の操作すべてを実施し、いずれも対象のみ変化することをDBで確認：(1) 通報のステータス変更（1件のみ変化、他3件は不変）、(2) 「募集を非公開にする」→確認ダイアログ→実行で対象投稿のみ`CLOSED`/`DELETED_BY_ADMIN`（他の投稿は`OPEN`のまま）、(3) 「利用停止の解除」でユーザーIDを指定→確認ダイアログ→実行で対象ユーザーのみ`SUSPENDED`→`ACTIVE`（他ユーザーは不変）。解除後は当該ユーザーのプロフィールが再度閲覧可能になることも確認 |
| ST-049 login利用者が問い合わせを自由記述と画像で送る | PASS | header→お問い合わせから、改行を含む本文と実物のキャラクターアイコンPNG（合成の1x1等ではない）を添付して送信。完了メッセージ表示とフォームのクリアを確認。DBに`type='CONTACT'`、改行を保持した本文、実際にアップロードされた画像パスが保存されることを確認 |
| ST-050 login利用者が機能要望を別画面・種別で送る | PASS | header→機能要望は`/contact`とは別URL・別見出し（「要望の内容」）の画面で、画像なしで送信。DBに`type='FEATURE_REQUEST'`として保存され、直前のCONTACTレコードと混同されず独立して記録されることを確認 |
| ST-051 未login headerに問い合わせ・機能要望を表示しない | PASS | `/`・`/posts`・`/users/910004`の3画面すべてでheader/footer双方に「お問い合わせ」「機能要望」のリンクが一切存在しないことをDOM検索で確認。`/contact`・`/feature-request`への直URLアクセスはいずれも`/login?next=...`へリダイレクトされることを確認 |
| ST-052 管理者が問い合わせと要望の本文・画像を識別する | PASS | admin一覧（`/admin/reports`下部）で、ST-049/050で送信した新規分を含め新しい順に表示され、種別ラベル（「お問い合わせ」/「機能要望」）・投稿者名・本文・画像（添付があるものだけ）が正しく描画されることを確認 |
| ST-053 通知の自動追加・filter・個別既読・一括既読を操作する | PASS | U07で`/notifications`を開いた状態のまま、別の独立したHTTPセッション（cookieを共有しない、U08として個別にログインし直したセッション）から実際にDMを送信し、15秒ポーリングにより画面をリロードせず新着通知が自動追加され件数（1→2件の未読/2→3件）とheaderの未読dotが同期することを確認。個別「既読にする」（対象1件のみ既読化、他は不変）、「すべて既読にする」（3件とも既読化、トースト表示、ボタンdisabled化、未読dot非表示化）、「未読だけ」フィルタ（既読化後に0件表示へ切替）もすべて確認 |

## 発見した不具合

なし。PW-F全10ケースを実際の操作・DB照合込みで検証したが、ソースコードの変更を要する不具合は見つからなかった。

## 検証

- `mvnw.cmd clean test`（`DB_PASSWORD`設定、rebase後・PW-E検証と同一状態）: 14,322件実行 / 0 failures / 0 errors / 18 skipped（PW-F自体はソース変更なしのため新規実行はしていない。直近の`09055d4`時点のCI緑を維持）
- 本バッチはソースコード変更を伴わないため、追加のコミット・push・CI実行は行っていない

## 未実施・持ち越し

- モバイル/タブレット幅での再実施は未着手（PW-A〜Eから継続の持ち越し）
- PW-G・PW-H・PW-I（障害回復、権限・攻撃入力、a11y）は元々本タスクの対象範囲外
- ST-046で使用した通報対象（投稿P002=920002は非公開化済み、U11は利用停止解除済み等）は本セッション終了時のDB再投入（`seed-release-users.sql`）で元の状態にリセットされる
