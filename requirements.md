# Band Link 要件定義書

更新日: 2026-09-05。版: 0.2。状態: 機能要件の整理済み・実装前設計中。
本書単体でサービスの目的、確定仕様、開発方針、未決事項を把握できるようにする。Claude等へ渡す場合も会話履歴を前提としない。
本書は元の引き継ぎ資料を更新するものとし、変更点は本書を優先する。「確定」はユーザー回答に基づく仕様、「設計案」は今後具体化する提案、「未決」はまだ選択されていない事項を意味する。本文の機能要件は特記がなければ確定事項。

## 0. 初めて読む人・AIへの案内

現在はアプリ本体の実装前。旧アプリは存在するが、新アプリの実装・CI・ログ基盤・テスト環境が完成しているわけではない。本書にある「提供する」は完成報告ではなく、作る対象を示す。

読み順は、本書 → `docs/development-workflow.md` → `DESIGN.md`。UI作業ではさらに `docs/design-references/` の3原文を全文読む。AIの作業入口は `AGENTS.md`。詳細DBスキーマ・新API契約・画面モックは今後作成する。

旧資料から特に変わった点は、未ログイン閲覧、メール確認・再設定、退会、ブロック・通報・管理画面、検索条件の拡張、30日の掲載期限、再公開時の順位制限、アプリ内自動更新、ログ基盤の今回導入。旧コードの挙動で本書を上書きしない。

用語: 「終了」は募集を一覧から外して詳細を残す状態。「再公開」は終了した募集を再び一覧へ戻す操作。「順位更新」は一覧で新しい掲載として扱う日時の更新であり、再公開の可否とは別。「ST」はブラウザから一連の業務を確認するシステムテスト。

## 1. 目的と開発範囲

- 実際にバンド仲間を募集するサービスとして運営する掲示板型Webアプリ。
- アプリ名は **Band Link（バンドリンク）**。
- 「メンバー募集」「参加希望」の投稿と、ユーザー間の直接メッセージを提供する。
- 旧band-recruitmentアプリの仕様確認はここまでの要件定義で完了済み。以降の設計・実装では旧コード・旧プロジェクトを参照・比較材料にしない（2026-09-05変更）。
- 新規作業先: `C:\Users\parus\Desktop\band`。
- 既存データは移行せず、新しいDBで開始する。既存DB・コード・画像は削除しない。
- まずローカルで全機能を動作させて確認し、その後に公開環境を準備する。

## 2. 技術・設計方針

- Java / Spring Boot / Spring Security / Spring Data JPA・Hibernate / PostgreSQL。
- Thymeleaf画面と `/api/**` のJSON APIを併存させる。素のCSSを使用する。
- セッション認証、BCryptによるパスワード保存。
- Controller → Service → Repositoryに分離し、業務ルールとトランザクション制御はServiceに集約する。
- DTOのBean Validationと例外ハンドラにより入力エラーを統一する。
- エラーは機械判定用コードと表示メッセージを持つ構造化レスポンスとする。
- 検索・ページングはDBで行い、全件を取得してJavaで絞り込む旧実装を変更する。
- 画像はローカルファイルシステムに保存する。
- 秘密情報は環境変数で管理する。公開リポジトリへの格納は禁止。
- 変更はmainへ直接反映する（2026-09-06変更）。PRとレビューは必須とせず、代わりに反映前のJUnit成功を条件とする。
- 元の資料にあるCSRF保護・画像検証・構造化ログの改善は、具体設計で扱う。
- Java / Spring Bootの採用バージョンは実装設計時に確定する。既存pomはJava 26 / Spring Boot 4.1.0。

## 3. 閲覧・認証・アカウント

- 未ログインでも募集一覧・投稿詳細・公開プロフィールを閲覧できる。
- 投稿とメッセージ送信にはログインおよびメールアドレス確認が必要。
- 登録、ログイン、ログアウト、自分の情報取得を提供する。
- メールアドレス確認とパスワード再設定を初回公開に含める。
- 登録直後のメールアドレス未確認セッションは、本登録確認画面だけを表示する。募集・検索・プロフィール・メッセージ・通知など他の画面へは移動できず、メール内リンクで確認が完了した後に通常利用へ戻る。
- 利用年齢の制限は設けない。
- LINE Loginによる外部認証を提供する。LINEで初回ログインしたユーザーはLINEの表示名でアカウントを作成し、以後はLINEアカウントでログインできる。
- 退会機能を提供する。本人が退会した場合はプロフィール、募集、会話、メッセージを削除し、同じメールアドレスで再登録できるようにする。
- 運営による利用停止では、既存の会話を残し、送信者名を「利用停止中ユーザー」に置き換える。
- 利用停止時はプロフィールと募集を非公開にし、投稿・メッセージ送信を禁止する。
- 利用停止中のログイン後画面には停止案内と問い合わせ先だけを表示する。解除は運営が行う。

## 4. プロフィール

- 表示名、活動エリア、担当パート、好きなジャンル、活動スタンス、経験年数、性別、演奏動画、画像を扱う。
- メールアドレス等は自分専用情報とし、公開レスポンスに含めない。
- プロフィール登録時に担当パート、好きなジャンル、活動エリア、活動スタンス、経験年数、年齢、性別を必須入力とする。活動エリアは1つ以上3つまで、性別は「男」「女」から選択する。
- 年齢は入力した値を「○歳」として公開表示する。
- 自己紹介は任意、1,000文字まで。
- プロフィール画像は1枚、5MBまで。
- YouTubeの演奏動画はプロフィール内で再生し、それ以外のURLは外部リンクとして表示する。
- 募集投稿の有無に関係なく、公開プロフィールからメッセージを開始できる。

## 5. 募集投稿

- 種別は「メンバー募集」「参加希望」。
- 募集タイトルは30文字以内、本文は500文字以内。
- タイトル・本文・活動エリア・パート・ジャンル・活動スタンスを必須とする。
- パート・ジャンル・活動スタンスはそれぞれ1つ以上選ぶ。
- 参加希望のパートは、自分が担当したいパートを表す。
- 活動エリアは全国の都道府県から3つまで選択し、市区町村・駅などを自由記入できる。
- 希望年齢層は年代を複数選択でき、「年齢不問」も選べる。
- 活動頻度は「週2回以上・週1回程度・月2〜3回・月1回程度・不定期・相談して決める」から1つ選ぶ。
- 募集画像は5枚まで、1枚5MBまで。新規作成時は本文と画像をまとめて登録できる。
- 投稿者本人だけが編集・削除・終了・再公開できる。
- 同時公開は1ユーザー1件まで。別の公開投稿がある場合は新規作成・再公開不可。
- 新規投稿・編集後の12時間は、そのユーザーの新規投稿・編集を禁止する。
- 画像の追加・削除・並べ替えも編集として扱い、同じ12時間制限に含める。本文・条件・画像はまとめて保存する。
- 終了・再公開はこの編集制限の対象外。
- 投稿は公開から30日で自動終了する。再公開すると掲載期限をその時点から30日に更新する。
- 編集では掲載順位を変えない。再公開時に順位を上げられるのは12時間に1回まで。
- 再公開の可否と掲載順位の更新可否は別に判定する。
- 終了した募集は一覧から外すが、詳細URLでは本文・画像と「募集終了」を表示する。
- 終了した募集の投稿者にもプロフィールから連絡できる。
- 削除・退会・運営による非公開は通常の募集終了と区別する。

## 6. 検索・一覧・履歴

- 公開中の投稿を対象に、募集種別・都道府県・パート・活動スタンス・ジャンル・キーワード・希望年齢層・活動頻度で検索する。
- キーワードの対象はタイトルと本文。
- 都道府県・パート・ジャンル・活動スタンス・希望年齢層・活動頻度は複数選択可能。
- 同じ項目内はOR、異なる項目間はANDで一致判定する。
- 一覧は下端へのスクロールで次の投稿を自動追加表示する。
- ログイン中に条件を指定して検索した場合だけ、全検索条件を履歴に保存する。
- 検索履歴はユーザーごとに直近5件。同じ条件は重複保存せず最新へ移動する。
- ページ移動・続きを読み込む操作では履歴を追加しない。
- お気に入り機能は対象外。

## 7. 会話・通知

- 2ユーザー間で会話は1つ。最初の送信時に作成する。
- 自分自身への送信は禁止。本文か画像の少なくとも一方が必要。
- メッセージ画像は送信1回につき1枚、5MBまで。
- メッセージ本文は1,000文字以内。入力上限を超えた場合は理由が分かるエラーを表示する。
- 会話一覧は最終メッセージ日時順。会話の閲覧・既読操作は参加者だけに許可する。
- 相手が読んだメッセージには送信者側でも「既読」を表示する。
- 新着メッセージとアプリ内通知は自動更新する。会話画面はページを再読み込みせず即時反映する。
- メッセージのメール通知は提供しない。認証・復旧用メールは提供する。
- メッセージ送信時に相手へのNEW_MESSAGE通知を作る。
- 募集終了後も既存の会話を継続できる。

## 8. ブロック・通報・管理

- 初回公開にブロック・通報・最小限の管理画面を追加する（旧スコープから変更）。
- ブロック関係がある場合は双方のメッセージ送信を禁止する。
- ログイン中の募集一覧では双方に相手の投稿を表示しない。過去の会話は残す。
- 公開情報はログアウトすれば閲覧できるため、ブロックを完全な閲覧禁止とは扱わない。
- 投稿・ユーザー・メッセージの通報を受け付ける。
- メッセージ通報で運営に見せるのは、通報された本文・画像と通報理由だけ。前後の会話は含めない。
- 管理画面で通報確認、投稿削除、利用停止・解除を行う。

## 9. 初期マスタ（承認済み）

- パート: ボーカル、ギター、ベース、ドラム、キーボード、DJ、管楽器、弦楽器、パーカッション、その他。
- ジャンル: ポップス、ロック、ハードロック／メタル、パンク、ジャズ、ブルース、ファンク／ソウル、R&B、ヒップホップ、電子音楽、フォーク／カントリー、クラシック、アニソン／ゲーム音楽、その他。
- 活動スタンス: 趣味で楽しみたい、趣味でも本格的に取り組みたい、プロを目指したい、プロとして活動中。
- 後から選択肢を追加できる設計にする。
- 既存ソース内には初期データ定義が見つからなかったため、上記を新規データとして採用する。

## 10. 必要な画面

登録、メール確認、ログイン、パスワード再設定、募集一覧・詳細・作成・編集、自分の投稿管理、公開プロフィール、プロフィール編集、会話一覧・会話、通知、ブロック管理、通報フォーム、退会、利用停止案内、運営用管理画面。
画面パスと遷移の詳細は次の設計で決定する。

## 11. 保留事項・次の設計作業

- SoundCloud・Dribbble・Contraの参考MDは受領・保存済み。全体はSoundCloud、検索・募集一覧はDribbble、プロフィールの情報整理はContraを参考にする。具体的な色・寸法・最終テーマはモックで確認する。
- 公開環境・ドメイン・実メール配信サービスは、ローカル確認後の段階で決める。
- DBスキーマとAPI仕様は本書に合わせて更新する。旧APIの単一areaや検索条件、認証必須設定をそのまま引き継がない。
- 画像の許可形式、文字数上限（自己紹介以外）、ページ取得件数、自動更新間隔は実装案として提示する。
- 希望年齢層の「年齢不問」や任意未入力値が検索に一致する条件を整理する。
- 再公開の順位更新基準をユーザー単位でどう管理するか、新規投稿直後との整合を具体化する。
- 管理権限の付与方法、問い合わせ先、通報処理の状態と記録保持を具体化する。
- 年齢の保存方法と年代更新、プロフィールの活動エリアの選択数は具体設計で整理する。
- CSRF、画像実体検証、会話画像の参加者認可、並行操作時の公開件数制限を設計・検証する。
- Elasticsearch / Kibanaの採用は確定し、今回のローカル開発基盤に含める。収集方式・バージョン・保持期間・リソース配分は設計で確定する。

## 12. 主要な受け入れ確認

- 未ログインで公開情報を閲覧でき、未確認メールのユーザーは投稿・送信できない。
- 同時操作でも公開投稿が1人1件を超えず、新規・編集・画像変更の12時間制限が一致する。
- 編集で順位が変わらず、再公開の順位更新制限と30日の期限が独立して正しく動く。
- 複数条件検索・無限スクロール・重複しない5件の検索履歴が動く。
- 会話の自動更新と既読が動き、第三者は会話と非公開画像を取得できない。
- ブロック、退会、利用停止に応じた表示・送信制限が働く。
- 通報管理に前後の私的会話が表示されない。
- 登録確認・パスワード再設定をローカルで検証できる。

## 13. 開発方式（採用確定）

### 13.1 GitHubと役割分担

- **mainへ直接コミット・pushする（2026-09-06変更）。** 個人開発ではPRの往復が作業の主な待ち時間になっていたため、PRとブランチ保護は必須としない。
- 代わりの品質条件：pushの前にJUnitを実行し、成功を確認する。CIはmainへのpushで動くので、失敗したら追いかけて直す。
- 影響範囲が読みにくい大きな変更に限り、`feature/<name>`ブランチとPRを使ってよい。判断は作業者に任せる。
- 進行・設計・実装・レビュー・検証の役割分担は維持する。ただしレビューはPR上ではなく作業内で行う。

### 13.2 テスト

- 単体テストは **JUnit**。投稿制限、権限、検索条件、状態遷移などの業務ルールを確認する。
- STは **Playwright MCP**。複数ユーザーによる登録・投稿・検索・会話・ブロック・管理操作をブラウザで確認する。
- STの手順、期待結果、実結果、証跡、対象リビジョンを残す。実行していないテストは未実行と記す。
- Playwright MCPによるエージェントの操作と、CIで動くPlaywrightテストコードは別物。後者を採用済みと扱わない。
- DBを含む結合テストの方式は設計案として別途具体化する。

### 13.3 Elasticsearch・Kibanaによるログ管理

- アプリのログをElasticsearchへ収集し、Kibanaで検索・確認できる環境を今回用意する。
- 募集検索の保存先はPostgreSQL。Elasticsearchの今回の目的はログ管理。
- 設計案: 構造化JSONログに日時・レベル・処理名・リクエストID・結果・所要時間を記録する。
- 設計案: パスワード、認証トークン、Cookie、メッセージ本文、リクエスト本文全体はログに記録しない。例外出力も同じ基準で扱う。
- ログ収集エージェント、保存期間、障害時の挙動、Docker構成、資源使用量は導入前に設計する。Logstashの採用は未確定。

### 13.4 ハーネスエンジニアリング

AIが変更を自分で検証し、不具合を再現できる開発環境を整える。ローカル起動・停止、テストデータ準備、JUnit実行、ST実行手順、ログ調査、CIを再現可能にする。具体的なコマンドは環境を構築して動作確認してから記載する。

### 13.5 コンテキストエンジニアリング

会話中だけに仕様を残さず、要件・設計・判断理由・進捗・検証結果をファイルで管理する。`AGENTS.md`から必要な資料へ案内し、担当ごとに読む資料と変更範囲を明確にする。設計変更時は理由と影響を記録し、要件とコードの食い違いを放置しない。

### 13.6 プロンプトエンジニアリング

担当エージェントへの依頼は、目的、対象資料、制約、受け入れ条件、検証、出力形式を含む標準テンプレートで作成する。確定事項・提案・未決事項を分け、実装者とレビュー担当の依頼を独立させる。

### 13.7 ループエンジニアリング

機能単位で「要件選択 → 設計 → 実装 → JUnit → 独立レビュー → Playwright MCPによるST → Elasticsearch/Kibanaでログ確認 → 修正・再検証」を繰り返す。1ループは通常1つのユースケースまたは業務ルールに限定する。各段階の状態、必須成果物、失敗時の戻り先、3回同じ失敗をした場合のエスカレーションを `docs/engineering-method.md` に定める。ループの終了条件は受け入れ条件の達成、重大指摘の解消、必要な検証結果の記録。未実行の検証は成功として扱わず、失敗は再現条件・原因・修正・再検証結果を文書化する。

4方式の詳細と担当間の受け渡しは `docs/engineering-method.md` にまとめる。

## 14. 実装前の順序と完了基準

1. 本書と開発方針を最新版にそろえる（今回の文書更新）。
2. 募集一覧・プロフィール・メッセージのモックを作り、画面構成と見た目を確認する。
3. DB・API・権限・状態遷移を具体設計する。第11章の未決事項は判断理由を付けて解消する。
4. ブランチ運用、ローカル環境、JUnit・CI、Playwright MCP、ログ収集の土台を構築・検証する。
5. 機能単位で実装 → 独立レビュー・検証 → 修正 → 再確認 → PRマージへ進む。

ローカル完成は、機能が動くだけでなく、主要受け入れ確認とJUnit・STの結果があり、Kibanaで処理を追え、別の担当が文書から起動・検証を再現できる状態とする。インターネット公開は次の段階。
