package com.example.bandlink.repository; import com.example.bandlink.entity.Notification; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface NotificationRepository extends JpaRepository<Notification,Long>{List<Notification> findByUserIdOrderByCreatedAtDesc(Long id);long countByUserIdAndReadAtIsNull(Long id);long countByUserIdAndTypeAndReadAtIsNull(Long id,String type);Optional<Notification> findByIdAndUserId(Long notificationId,Long userId);}
