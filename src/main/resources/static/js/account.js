import {api,h,icon,avatar,state,main,showPage,notice,empty,button,toast,bindForm,confirmAction,time,choices,chips,counter,requireUser,verificationNotice} from './ui.js';

const fields=[['prefectureIds','活動エリア','prefectures'],['partIds','パート','parts'],['genreIds','ジャンル','genres'],['stanceIds','活動スタンス','stances']];
const videoHref = value => { try { const u=new URL(value); return ['http:','https:'].includes(u.protocol)?u.href:''; } catch { return ''; } };
const youtubeId = value => { try {const u=new URL(value); if(!['youtube.com','www.youtube.com','youtu.be'].includes(u.hostname))return ''; return u.hostname==='youtu.be'?u.pathname.slice(1):u.searchParams.get('v')||u.pathname.split('/').pop();}catch{return '';} };
const names = items => (items||[]).map(x=>x.name).join('・');
export function accountPage(path){
  if(path==='/login'||path==='/register'||path==='/verify-email'||path==='/password-reset'||path==='/password-reset/confirm'){authPage(path);return true;}
  if(path==='/settings'||path==='/settings/profile'){if(requireUser()) profileEdit();return true;}
  if(path==='/settings/blocks') return false;
  if(path==='/support'){supportPage();return true;}
  if(/^\/users\/\d+$/.test(path)){profilePage(path.split('/')[2]);return true;}
  return false;
}
async function authPage(path){
  const next=new URLSearchParams(location.search).get('next')||'/posts';
  const config={
    '/login':['ログイン','Band Linkへ戻る','メールアドレスとパスワードでログインします。'],
    '/register':['アカウントを作成','音楽のつながりをはじめる','活動エリアや好きなジャンルから、あなたに合う仲間を探せます。'],
    '/verify-email':['メールアドレスを確認','もう少しで準備完了です','登録時に届いた確認トークンを入力してください。'],
    '/password-reset':['パスワードを再設定','また音楽の時間へ','登録メールアドレスに再設定用の案内を送ります。'],
    '/password-reset/confirm':['新しいパスワードを設定','安全なパスワードに更新します','届いたトークンと新しいパスワードを入力してください。']
  }[path];
  const register=path==='/register', verify=path==='/verify-email', reset=path==='/password-reset', confirm=path==='/password-reset/confirm';
  let form='';
  if(path==='/login') form=`<form id="auth-form"><div class="form-field"><label for="email">メールアドレス</label><input class="input" id="email" name="email" type="email" autocomplete="email" required></div><div class="form-field"><label for="password">パスワード</label><input class="input" id="password" name="password" type="password" autocomplete="current-password" minlength="8" required></div><button class="button primary full" type="submit">ログイン</button></form>`;
  if(register) form=`<form id="auth-form"><div class="form-field"><label for="username">表示名</label><input class="input" id="username" name="username" maxlength="80" autocomplete="nickname" required placeholder="活動名やニックネーム"></div><div class="form-field"><label for="email">メールアドレス</label><input class="input" id="email" name="email" type="email" maxlength="320" autocomplete="email" required></div><div class="form-field"><label for="password">パスワード</label><input class="input" id="password" name="password" type="password" minlength="8" maxlength="128" autocomplete="new-password" required><span class="hint">8文字以上で設定してください。</span></div><button class="button primary full" type="submit">アカウントを作成</button></form>`;
  if(verify) form=`<form id="auth-form"><div class="form-field"><label for="token">確認トークン</label><input class="input" id="token" name="token" maxlength="100" required autocomplete="one-time-code" placeholder="メールに記載されたトークン"></div><button class="button primary full" type="submit">メールアドレスを確認</button></form>`;
  if(reset) form=`<form id="auth-form"><div class="form-field"><label for="email">メールアドレス</label><input class="input" id="email" name="email" type="email" autocomplete="email" required></div><button class="button primary full" type="submit">再設定メールを送る</button></form>`;
  if(confirm) form=`<form id="auth-form"><div class="form-field"><label for="token">再設定トークン</label><input class="input" id="token" name="token" maxlength="100" required></div><div class="form-field"><label for="newPassword">新しいパスワード</label><input class="input" id="newPassword" name="newPassword" type="password" minlength="8" maxlength="128" autocomplete="new-password" required></div><button class="button primary full" type="submit">パスワードを更新</button></form>`;
  showPage(`<div class="page auth-layout"><section class="auth-story"><span class="eyebrow">${register?'FIND YOUR PEOPLE':'WELCOME BACK'}</span><h1>${h(config[0])}<br><span class="muted">${register?'好きな音を、好きな人と。':'あなたの音楽の場所へ。'}</span></h1><p>${h(config[2])}</p><div class="steps"><div><span>01</span><p>好きな条件で募集を探す</p></div><div><span>02</span><p>プロフィールから声をかける</p></div><div><span>03</span><p>一緒に音を鳴らす</p></div></div></section><section class="panel auth-panel"><span class="eyebrow">BAND LINK</span><h2>${h(config[0])}</h2><p class="muted">${h(config[1])}</p><div id="auth-message"></div>${form}<div class="auth-footer">${path==='/login'?`アカウントをお持ちでない方は <a href="/register">新規登録</a><br><a href="/password-reset">パスワードを忘れた方</a>`:register?`すでに登録済みの方は <a href="/login">ログイン</a>`:reset?`<a href="/login">ログインへ戻る</a>`:confirm?`<a href="/login">ログインへ戻る</a>`:`<a href="/login">ログインへ戻る</a>`}</div></section></div>`,config[0]);
  bindForm(main.querySelector('#auth-form'),async fd=>{
    let response;
    if(path==='/login') response=await api('/api/auth/login',{method:'POST',body:{email:fd.get('email'),password:fd.get('password')}});
    else if(register) response=await api('/api/auth/register',{method:'POST',body:{username:fd.get('username'),email:fd.get('email'),password:fd.get('password')}});
    else if(verify){await api('/api/auth/verify-email',{method:'POST',body:{token:fd.get('token')}});main.querySelector('#auth-message').innerHTML=notice('メールアドレスを確認しました。ログインすると募集を投稿できます。','success');return;}
    else if(reset){await api('/api/auth/password-reset/request',{method:'POST',body:{email:fd.get('email')}});main.querySelector('#auth-message').innerHTML=notice('再設定の案内を送信しました。メールをご確認ください。','success');return;}
    else {await api('/api/auth/password-reset/confirm',{method:'POST',body:{token:fd.get('token'),newPassword:fd.get('newPassword')}});main.querySelector('#auth-message').innerHTML=notice('パスワードを更新しました。ログインしてください。','success');return;}
    if(response) {state.user=response;toast(register?'アカウントを作成しました。':'ログインしました。');location.assign(next);}
  });
}
async function profilePage(id){
  try {const p=await api('/api/users/'+Number(id));const own=state.user&&String(state.user.id)===String(p.id);const video=videoHref(p.videoUrl), yt=youtubeId(p.videoUrl);showPage(`<div class="page"><a class="back-link" href="/posts">${icon('back')}募集一覧へ</a><div class="profile-layout"><aside class="profile-identity">${avatar(p,true)}<h1>${h(p.username)}</h1><p class="muted">${h(names(p.prefectures)||'活動エリア未設定')}</p>${own?button('プロフィールを編集','/settings/profile','secondary'):button('メッセージを送る',state.user?'/messages?to='+p.id:'/login?next='+encodeURIComponent('/messages?to='+p.id))}${!own&&state.user?'<button class="button quiet small" id="report-user">このプロフィールを通報</button>':''}</aside><article class="stack"><section><span class="eyebrow">ABOUT THE MUSIC</span><h2>プロフィール</h2><dl class="facts" style="margin-top:24px"><dt>パート</dt><dd>${h(names(p.parts)||'未設定')}</dd><dt>ジャンル</dt><dd>${h(names(p.genres)||'未設定')}</dd><dt>経験年数</dt><dd>${p.experienceYears==null?'未設定':h(p.experienceYears)+'年'}</dd><dt>活動スタンス</dt><dd>${h(names(p.stances)||'未設定')}</dd><dt>年代</dt><dd>${h(p.ageRange||'非公開')}</dd></dl></section><section class="detail-section"><h2>自己紹介</h2><p class="body-text">${h(p.bio||'自己紹介はまだありません。')}</p></section>${video?`<section class="detail-section"><h2>演奏動画</h2>${yt?`<iframe class="video" src="https://www.youtube-nocookie.com/embed/${h(yt)}" title="${h(p.username)}の演奏動画" loading="lazy" allowfullscreen></iframe>`:`<a class="row" href="${h(video)}" target="_blank" rel="noopener noreferrer">${icon('external')}演奏動画を開く</a>`}</section>`:''}</article></div></div>`,p.username);main.querySelector('#report-user')?.addEventListener('click',()=>report('USER',id));}
  catch(e){showPage(`<div class="page">${empty('プロフィールを表示できません。',h(e.message),button('募集を探す','/posts','secondary'))}</div>`,'プロフィール');}
}
async function profileEdit(){
  const [p,m]=await Promise.all([api('/api/users/me'),api('/api/masters')]);
  const section=(key,label,source)=>`<div class="form-field"><span class="form-label">${label} <span class="optional">任意</span></span>${source==='prefectures'?`<select class="input" name="${key}" multiple size="5">${m[source].map(x=>`<option value="${x.id}" ${p[source]?.some(y=>y.id===x.id)?'selected':''}>${h(x.name)}</option>`).join('')}</select><span class="hint">活動エリアは3つまで。</span>`:choices(key,m[source],p[source]?.map(x=>x.id)||[])}</div>`;
  showPage(`<div class="page narrow"><a class="back-link" href="/users/${p.id}">${icon('back')}プロフィールへ戻る</a><div class="page-heading"><div><span class="eyebrow">YOUR PROFILE</span><h1>プロフィールを編集</h1><p>あなたの音楽のことを、仲間に伝えましょう。</p></div></div>${verificationNotice()}<form id="profile-form"><fieldset class="form-section"><legend>基本情報</legend><div class="stack"><div class="form-field"><label for="username">表示名</label><input class="input" id="username" name="username" maxlength="80" required value="${h(p.username)}"></div><div class="form-field"><label for="bio">自己紹介 <span class="optional">任意・1,000文字まで</span></label><textarea class="input" id="bio" name="bio" maxlength="1000" rows="7" placeholder="好きな音楽、これまでの経験、やってみたいことなど。">${h(p.bio)}</textarea><span class="hint" data-count="bio"></span></div><div class="form-grid"><div class="form-field"><label for="age">年齢 <span class="optional">任意・公開は年代のみ</span></label><input class="input" id="age" name="age" type="number" min="0" max="120" value="${p.age??''}"></div><div class="form-field"><label for="experienceYears">経験年数 <span class="optional">任意</span></label><input class="input" id="experienceYears" name="experienceYears" type="number" min="0" max="100" value="${p.experienceYears??''}"></div></div><div class="form-field"><label for="gender">性別 <span class="optional">任意</span></label><input class="input" id="gender" name="gender" maxlength="40" value="${h(p.gender)}"></div></div></fieldset><fieldset class="form-section"><legend>音楽と活動エリア</legend><div class="stack">${fields.map(x=>section(...x))}</div></fieldset><fieldset class="form-section"><legend>演奏動画</legend><div class="form-field"><label for="videoUrl">YouTubeなどのURL <span class="optional">任意</span></label><input class="input" id="videoUrl" name="videoUrl" type="url" maxlength="1000" value="${h(p.videoUrl)}" placeholder="https://youtu.be/..."></div></fieldset><div class="sticky-actions">${button('キャンセル','/users/'+p.id,'secondary')}<button class="button primary" type="submit">変更を保存する</button></div></form><section class="detail-section"><h2>アカウント</h2><p class="muted">退会するとプロフィールと募集は公開を終了します。送信済みメッセージは相手側に残ります。</p><div class="row" style="margin-top:18px"><button class="button danger" type="button" id="withdraw">退会する</button><button class="button secondary" type="button" id="logout">ログアウト</button></div></section></div>`, 'プロフィール編集');counter(main.querySelector('#profile-form'));
  bindForm(main.querySelector('#profile-form'),async fd=>{const body={username:fd.get('username').trim(),bio:fd.get('bio').trim(),age:fd.get('age')?Number(fd.get('age')):null,experienceYears:fd.get('experienceYears')?Number(fd.get('experienceYears')):null,gender:fd.get('gender').trim(),videoUrl:fd.get('videoUrl').trim()||null};for(const [key] of fields)body[key]=fd.getAll(key).map(Number);if(body.prefectureIds.length>3)throw new Error('活動エリアは3つまで選択できます。');await api('/api/users/me',{method:'PUT',body});toast('プロフィールを更新しました。');location.assign('/users/'+p.id);});
  main.querySelector('#logout').onclick=()=>confirmAction('ログアウトしますか？','次回はメールアドレスとパスワードでログインできます。',async()=>{await api('/api/auth/logout',{method:'POST'});location.assign('/login');});
  main.querySelector('#withdraw').onclick=()=>confirmAction('退会しますか？','プロフィールと募集は公開を終了し、アカウントを復元できません。送信済みメッセージは相手側に残ります。',async()=>{await api('/api/auth/withdraw',{method:'POST'});state.user=null;location.assign('/');});
}
function supportPage(){showPage(`<div class="page narrow"><div class="page-heading"><div><span class="eyebrow">NEED A HAND?</span><h1>ヘルプ・お問い合わせ</h1><p>Band Linkの使い方と、運営への連絡先。</p></div></div><div class="help-grid"><section class="panel"><h2>メールアドレスの確認</h2><p>登録後に届いた確認トークンを、メール確認画面へ入力してください。確認後に募集投稿とメッセージ送信ができます。</p></section><section class="panel"><h2>困ったときは</h2><p>不適切な募集やメッセージは、各画面の「通報」から運営へ知らせてください。緊急時はサービスの利用を中止し、運営へご連絡ください。</p></section></div></div>`,'ヘルプ・お問い合わせ');}
